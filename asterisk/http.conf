; Asterisk HTTP Configuration for VoiceAI Platform
; Required for ARI WebSocket and REST API

[general]
enabled = yes
bindaddr = 0.0.0.0
bindport = 8088
prefix = 
enablestatic = no

; TLS Configuration (optional, recommended for production)
; tlsenable = yes
; tlsbindaddr = 0.0.0.0:8089
; tlscertfile = /etc/asterisk/certs/asterisk.pem
; tlsprivatekey = /etc/asterisk/certs/asterisk.key
